(Computer environment check) print (\n) print
(computer name is ") host-name sys-info concat (".) concat print (\n) print
(operating system is ) os-type sys-info concat (.) concat print (\n) print
(cpu has ) cpu-num sys-info concat ( core.) concat print (\n) print
(cpu speed is ) cpu-speed sys-info concat (.) concat print (\n) print
(memory size is ) mem-size sys-info concat (KB.) concat print (\n) print
(memory used rate is ) mem-used sys-info mem-size sys-info div 100 mul concat (%.) concat print (\n) print