(../library/macro.stk) read-file eval
(../library/list.stk) read-file eval

1 10 1 range (count1) (
    1 10 1 range (count2) (
        ({count1 count2 mul 2 just eval} | ) format eval print
    ) for (\n) print
    (-) 44 repeat println
) for