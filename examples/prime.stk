(..\library\math.stk) read-file eval

(素数判定プログラム) print
(
    (> ) input (target) var
    
    (
        (終了します) print 
        0 exit
    ) (
        now-time (start) var
        ( target (は素数です（処理時間) concat time concat (秒）)　concat print )    
        ( target (は素数ではありません（処理時間)  concat time concat (秒）) concat print )
        target prime? eval
        now-time start sub (time) var
        if    
    )
    target (exit) find if
) (true) while
