( (indent) var
    ls sort (i) (
        (
            (|  ) indent 1 sub repeat (+-- ) i concat concat print
            i cd
            indent 1 add tree eval
            indent 1 sub (indent) var
            (..) cd
        ) (
            (|  ) indent 1 sub repeat (|-- ) i concat concat print
        ) i folder if
    ) for
) tree var
((Error! directory path is wrong.) print 0 exit) ()
args-cmd 2 get copy print cd (error:cd) equal if
1 tree eval