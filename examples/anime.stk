(
    ((as) true while)
    (as) song.stk read-file replace eval
) thread

(
    anime.txt read-file (split) split (i) (
        1 800 1 range (_) (() print) for
    　　i print
        .7 sleep
    ) for
) true while
