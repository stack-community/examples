(
    (([insider data of the file]) print (\n) print data print (\n) print)
    ((Please again.) print (\n) print view eval)
    (Enter open file > ) input 
    read-file (data) var 
    data (error:read-file) equal not if
) (view) var

view eval