(..\library) cd
ls (i) (
    i read-file eval
) for

(Stackプログラミング言語) print (
    () (prog) var (##) (in) var (
        (> ) input (in) var
        prog in ( ) concat concat (prog) var
    ) (in () equal not) while 
    prog eval print
) true while