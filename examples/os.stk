cls (\n) 15 repeat print
( ) 60 repeat print (Stack OS) print (\n) print () input

cls (\n) 15 repeat print
( ) 60 repeat print (login: ) input (name) var (\n) print
( ) 57 repeat print (Welcome to ) host-name sys-info concat print (\n) print () input
print cls

(
    [name host-name sys-info] (@) join (:~$ ) concat
    input eval print (\n) print
) true while
