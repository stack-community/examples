(
    (key) var (str) var
    str (c) (
        c encode key add decode
    ) map () join
) (encryption) var

(
    (key) var (codes) var
    codes (i) (
        i encode key sub decode
    ) map () join
) (decryption) var