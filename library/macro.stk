(
    (default) var
    (patern) var
    (value) var
    
    default (return) var
    patern (,) split (token) (
        token (=>) split (token) var
        (token 1 get eval (return) var)
        () value token 0 get eval find if
    ) for
    
    (token) free
    (patern) free
    (default) free

    return eval
) 
(match) var
